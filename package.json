{
  "name": "fahrzeugverwaltung",
  "version": "1.0.0",
  "description": "Fahrzeug Plus 6 Enterprise - Fahrzeugverwaltungssoftware",
  "private": true,
  "workspaces": [
    "backend",
    "frontend-admin",
    "frontend-driver",
    "shared"
  ],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:admin": "cd frontend-admin && npm run dev",
    "dev:driver": "cd frontend-driver && npm run dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:driver\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend-admin && npm install && cd ../frontend-driver && npm install && cd ../shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


